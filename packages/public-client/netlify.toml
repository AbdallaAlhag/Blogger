# Public client deployment for Netlify
[build]
  BASE = "packages/private-client"
  PUBLISH = "packages/private-client/dist"
  COMMAND = "npm run build"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

# Dev settings (for local Netlify dev server)
[dev]
  command = "npm run dev"
  port = 5173
  publish = "dist"